#This file defines the GCS bucket where terraform will store the state of this project/environment

/*
terraform {
  backend "gcs" {
    bucket = "pr-ca-ecom-commerce-support-tf"
    prefix = "project_maas/state"
  }
}

provider google-beta {
  version = "~> 2.4"
}

provider "google" {
  version = "~> 3.53.0"
  project = var.project
}
*/
